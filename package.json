{
  "name": "micro-ecs-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "prepare": "husky",
    "ci": "pnpm install --frozen-lockfile"
  },
  "devDependencies": {
    "@byloth/eslint-config-typescript": "^3.2.2",
    "@eslint/compat": "^2.0.3",
    "eslint": "^9.39.4",
    "husky": "^9.1.7",
    "typescript": "^5.9.3"
  }
}
